package com.ergal.ezweb.core;

import static org.junit.Assert.*;

import java.util.HashMap;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class TestUrlMappingManager {
	public TestUrlMappingManager(){
		
	}
	@Before
    public void setUp() throws Exception {
    }

    @After
    public void tearDown() throws Exception {
    }

    @Test
    public void testGetMapping() {
    	/*String[] classAllName = new String[2];
    	classAllName[0] = "Article.class";
    	classAllName[1] = "User.class";
    	String packageName = "com.ergal.ezweb.example.pages";
    	HashMap<String, String> mapping = UrlMappingManager.getMapping(classAllName, packageName);
    	String articleClassName = mapping.get("/article/");
    	String userUrlClassName = mapping.get("/user/");
    	
    	assertEquals(mapping.size(),2);
    	assertEquals("Article.class", articleClassName);
    	assertEquals("User.class", userUrlClassName);*/
    }
}
